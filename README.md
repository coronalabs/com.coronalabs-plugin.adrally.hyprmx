Sources for the plugin `plugin.adrally.hyprmx`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.adrally.hyprmx" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
